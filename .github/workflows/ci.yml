name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: build
        run: echo ok
# left a note for myself
# check perf here
# off-by-one, fixed
# tidy up
# left a note for myself
# TODO clean this
# left a note for myself
